version: 2

build:
  os: ubuntu-22.04

sphinx:
  builder: html
  configuration: docs/conf.py
  fail_on_warning: true

python:
  version: 3.8
  install:
    # - requirements: docs/requirements.txt
    - method: pip
      path: .
      extra_requirements:
        - docs
